import styled from '@emotion/styled';

export const FoundSymbol = styled.svg`
	background: radial-gradient(
			circle,
			white 50%,
			currentColor 50%,
			currentColor 70%,
			transparent 70%
		)
		no-repeat white;
	border-radius: 50vmax;
	color: forestgreen;
	/* height: auto;
	left: 50%;
	opacity: 0.85;
	padding: 1rem;
	pointer-events: none;
	position: absolute;
	top: 50%;
	transform: translate(-50%, -50%);
	width: max(70%, 15vw); */
`;
