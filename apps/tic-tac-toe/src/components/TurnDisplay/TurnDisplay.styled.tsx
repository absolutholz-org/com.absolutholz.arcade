import { TypoSize, TypoWeight } from '@arcade/components';
import styled from 'styled-components';

export const TurnDisplay = styled.div`
	font-size: ${TypoSize.x300};
	font-weight: ${TypoWeight.Light};
	line-height: 1;
`;
