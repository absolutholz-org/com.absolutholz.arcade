import styled from "@emotion/styled";

import { CONTENT_STYLES, SPACING_STYLES } from "../placeholderStyles";

export const StoryPlaceholder = styled.div`
    ${SPACING_STYLES}
`;

export const StoryPlaceholder_Content = styled.div`
    ${CONTENT_STYLES}
`;
