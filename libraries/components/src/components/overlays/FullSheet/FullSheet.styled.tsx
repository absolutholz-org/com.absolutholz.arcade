import styled from 'styled-components';

import { SpacingSize } from '../../../enums';
import { Overlay } from '../Overlay';

export const FullSheet = styled(Overlay)`
	height: 100%;
	margin: auto;
	max-width: 90rem;
	width: 100%;
`;
