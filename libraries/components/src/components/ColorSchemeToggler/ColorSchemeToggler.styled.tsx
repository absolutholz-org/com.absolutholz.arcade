import styled from 'styled-components';

import { Button } from '../buttons/Button';
import { Icon } from '../Icon';

export const ColorSchemeToggler = styled(Button)`
	align-items: center;
	display: inline-flex;
	gap: 0.25em;
`;

export const ColorSchemeTogglerIcon = styled(Icon)``;

export const ColorSchemeTogglerText = styled.span``;
