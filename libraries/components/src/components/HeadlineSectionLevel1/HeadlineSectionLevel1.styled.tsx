import styled from 'styled-components';

import { TypoSize, TypoWeight } from '../../enums';

export const HeadlineSectionLevel1 = styled.div`
	font-size: ${TypoSize.x200};
	font-weight: ${TypoWeight.Light};
	line-height: 1.4;
`;
