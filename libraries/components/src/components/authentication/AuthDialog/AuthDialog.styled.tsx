import styled from 'styled-components';
import { space } from '../../../foundations';
import { Button } from '../../buttons/Button';

export const AuthDialogSecondaryButton = styled(Button)`
	margin-right: auto;
`;

export const AuthDialogForgottenPasswordButton = styled(Button)`
	margin-top: ${space(100)};
`;
